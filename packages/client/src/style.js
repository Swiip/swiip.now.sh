import { h } from "preact";
import picostyle from "picostyle";

export { keyframes } from "picostyle";

const style = picostyle(h);

export default style;

export const mediaSmall = "@media (max-width: 1000px)";
